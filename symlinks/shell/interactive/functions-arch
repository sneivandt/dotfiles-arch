#!/usr/bin/env bash

# Update mirrorlist
update-mirrors()
{
    sudo bash -c "curl -s 'https://www.archlinux.org/mirrorlist/?country=US&protocol=https&ip_version=4' | sed 's/^#Server/Server/' | rankmirrors -n 6 - > /etc/pacman.d/mirrorlist"
}